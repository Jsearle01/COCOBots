# `docs/ground-truth/` — MANIFEST

**This directory is gitignored** (Jay, 2026-07-31). Its contents are present on disk and are
**never pushed**. This manifest is the only tracked file here.

**Why this file exists.** An ignored directory is invisible to the Orchestrator reading `wip`, and
absent after a context reset (CLAUDE.md §9). Without a tracked manifest, a fresh context reading
the repo concludes this directory does not exist at all.

## Status as of 2026-08-01 — EMPTY

**This directory is empty.** Nothing has been placed in it yet. That is the expected state at the
end of dispatch A1; it is not a missing-file defect and not a blocked gate.

**Jay populates this directory.** Its contents are ground-truth material supplied by him, not
generated by any dispatch.

## ⚠ Required action when it is populated

**When Jay adds files here, this manifest MUST be updated in the same commit**, with — for each
file — filename, byte size, SHA-256, and one line on what it is and why it is not pushed. Follow
the format used in [`reference/pet/MANIFEST.md`](../../reference/pet/MANIFEST.md).

An out-of-date manifest is worse than no manifest: it tells a fresh context that files exist which
do not, or hides files that do. Treat the update as part of the placement, not as follow-up.

## Contents

| file | bytes | SHA-256 | what it is / why not pushed |
|---|---:|---|---|
| _(none yet)_ | — | — | — |

## Not to be confused with

- **`reference/pet/`** — also ignored, but already populated. Holds the pinned PET build receipt
  (`listing.txt`, `symbols.txt`) and the repaired PET sources. That is CLAUDE.md §2 **rank-3**
  ground truth and has its own manifest.
- **`docs/methodology/`** — tracked and pushed. Method documents, not ground truth.
